<style>
    .custom-lens-premium {
        --clp-surface: #ffffff;
        --clp-border: #dbe4f3;
        --clp-text: #1a2c52;
        --clp-muted: #6f7f9f;
        --clp-primary: #2f80ed;
        --clp-primary-2: #1e5fc7;
        --clp-success: #12a86b;
        --clp-soft: #edf4ff;
        --clp-soft-border: #d0e2ff;
        --clp-shadow: 0 14px 34px rgba(15, 32, 76, 0.09);
        border: 1px solid var(--clp-border);
        border-radius: 18px;
        overflow: hidden;
        background: var(--clp-surface);
        box-shadow: var(--clp-shadow);
        direction: rtl;
    }

    .custom-lens-premium > .card-header {
        background: linear-gradient(120deg, var(--clp-primary), var(--clp-primary-2)) !important;
        border-bottom: 0;
        color: #fff;
        display: flex !important;
        align-items: center;
        justify-content: space-between !important;
        gap: .75rem;
        direction: rtl !important;
        padding: 1rem 1.1rem;
    }

    .custom-lens-premium > .card-header .card-title {
        margin: 0 !important;
        color: #fff !important;
        font-size: 1.03rem;
        font-weight: 800;
        letter-spacing: .1px;
        text-align: right !important;
    }

    .custom-lens-premium > .card-body {
        background: #fff;
        padding: 1rem 1rem 1.15rem;
    }

    .custom-lens-premium #divjqGrid {
        border: 1px solid #dce5f5;
        border-radius: 12px;
        overflow: hidden;
        background: #fff;
        margin-bottom: .95rem;
    }

    .custom-lens-premium #gbox_jqGrid,
    .custom-lens-premium #gview_jqGrid,
    .custom-lens-premium .ui-jqgrid {
        border: 0 !important;
    }

    .custom-lens-premium .ui-jqgrid-hdiv {
        background: #f3f7ff;
        border-bottom: 1px solid #e0e9f9;
    }

    .custom-lens-premium .ui-jqgrid .ui-jqgrid-htable th {
        color: #1e3160;
        font-size: .78rem;
        font-weight: 800;
        border-inline-start-color: #e8effa !important;
        border-inline-end-color: #e8effa !important;
    }

    .custom-lens-premium .ui-jqgrid .ui-jqgrid-htable th div {
        height: auto !important;
        line-height: 1.3;
        padding: .15rem .1rem;
    }

    .custom-lens-premium .ui-jqgrid-btable tr.jqgrow td {
        border-color: #eef2fa !important;
        color: #24355d;
        font-size: .8rem;
        padding: .35rem .25rem;
    }

    .custom-lens-premium .ui-jqgrid-btable tr.jqgrow:nth-child(even) td {
        background: #fbfdff;
    }

    .custom-lens-premium .ui-jqgrid-btable tr.jqgrow:hover td {
        background: #f2f8ff !important;
    }

    .custom-lens-premium .form-group.text-center.mb-4 {
        margin-bottom: 0 !important;
    }

    .custom-lens-premium .btn {
        border-radius: 10px;
        padding: .45rem .9rem;
        font-size: .8rem;
        font-weight: 700;
        border: 1px solid transparent;
        transition: .18s ease;
    }

    .custom-lens-premium .btn-primary {
        background: #1f74e8;
        border-color: #1f74e8;
        color: #fff;
    }

    .custom-lens-premium .btn-primary:hover {
        background: #165fc2;
        border-color: #165fc2;
        color: #fff;
    }

    .custom-lens-premium .btn-success,
    .custom-lens-premium .btn-light-success {
        background: var(--clp-success);
        border-color: var(--clp-success);
        color: #fff;
    }

    .custom-lens-premium .btn-success:hover,
    .custom-lens-premium .btn-light-success:hover {
        background: #0f8d59;
        border-color: #0f8d59;
        color: #fff;
    }

    .custom-lens-premium .btn-light-primary {
        background: var(--clp-soft);
        border-color: var(--clp-soft-border);
        color: #2f69c9;
    }

    .custom-lens-premium .btn-light-primary:hover {
        background: #deecff;
        color: #2558a8;
    }

    .custom-lens-premium .small.text-muted {
        color: var(--clp-muted) !important;
        font-size: .73rem;
    }

    .custom-lens-premium #jqGrid button {
        width: 30px;
        height: 30px;
        border-radius: 8px;
        border: 1px solid #d8e4ff;
        background: #f5f8ff;
        color: #35548f;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        margin: 0 2px;
        transition: .15s ease;
    }

    .custom-lens-premium #jqGrid button:hover {
        background: #e8f0ff;
    }

    .custom-lens-premium #jqGrid button i {
        font-size: .88rem !important;
        line-height: 1;
    }

    .custom-lens-premium #jqGrid button[title*='Delete'],
    .custom-lens-premium #jqGrid button[title*='حذف'] {
        background: #fff1f1;
        color: #bf2f2f;
        border-color: #ffdede;
    }

    .custom-lens-premium #jqGrid button[title*='Delete']:hover,
    .custom-lens-premium #jqGrid button[title*='حذف']:hover {
        background: #ffe4e4;
    }

    .custom-lens-premium .custom-lens-link,
    .custom-lens-premium a.text-primary {
        color: #2372e7 !important;
        font-weight: 700;
        text-decoration: none;
    }

    .custom-lens-premium .custom-lens-link:hover,
    .custom-lens-premium a.text-primary:hover {
        color: #1b57ba !important;
        text-decoration: underline;
    }

    .custom-lens-premium .alert {
        border-radius: 10px;
        margin-top: .8rem;
    }

    @@media (max-width: 767.98px) {
        .custom-lens-premium > .card-header {
            padding: .75rem .85rem;
        }

        .custom-lens-premium > .card-header .card-title {
            font-size: .95rem;
        }

        .custom-lens-premium > .card-body {
            padding: .8rem;
        }
    }
</style>
